[build]
builder = "nixpacks"
buildCommand = "pnpm --filter @paperclipai/plugin-sdk build"

[deploy]
startCommand = "pnpm --filter @paperclipai/server exec tsx src/index.ts"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
